# Work Flow: Load data -> Load truck -> Create route -> Deliver packages: UI takes input and returns values
# Load data: Read from txt files into data structures and objects
# Load truck: Put packages into trucks per requirements of delivery times and notes
# Create route: Each truck routes by delivery time and distance from one location to the other
# Deliver packages: Each truck runs its route, one(or both) returns and gets last 8 packages -> Work Flow again
